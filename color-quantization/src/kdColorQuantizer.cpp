#include "kdColorQuantizer.h"

#include <algorithm>
#include <cmath>
#include <unordered_set>

void KdColorQuantizer::createPalette(std::vector<Color> &img, int numberOfColors) {
    // you can use an artificial set of colors to test your code
    // std::vector<Color> colors{Color(255, 10, 0), Color(255, 20, 0),
    //                          Color(240, 10, 0), Color(240, 20, 0),
    //                          Color(10, 255, 0), Color(20, 255, 0),
    //                          Color(10, 240, 0), Color(20, 240, 0)};

    // TODO
}

std::vector<Color> KdColorQuantizer::quantize(std::vector<Color> &img) {
    // TODO
    return img;
}
